#include "main.h"

/**
 * _abs - Prints the absolute value of an integer
 * @n: Parameter to be passed in
 *
 * Return: int
 */

int _abs(int n)
{

	if (n < 0)
	{
		return (-1 * n);
	}

	return (n);
}
